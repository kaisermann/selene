@import '../../config/setup'
@import '../../config/mixins'
@import '../../config/collections'
@import '../../config/fonts'
@import '../mixins.styl'

// Only build the admin css if app.buildLoginCSS is true
if app.buildCSS.login
	@import 'config'
	@import 'partials/core'
		
